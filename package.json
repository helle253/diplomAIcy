{
  "name": "diplomaicy",
  "version": "0.1.0",
  "description": "Agent-oriented Diplomacy client with spectator UI",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
